# country_code_data

A dataset containing full list of Country Codes. Use this dataset if
loading from the web is slow.

## Usage

``` r
data(country_code_data)
```

## Format

A data table with 239 rows and 2 variables
